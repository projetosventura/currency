version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - EMAIL_USER=${EMAIL_USER}
      - EMAIL_PASS=${EMAIL_PASS}
    depends_on:
      - db

  db:
    image: 'sqlite3'
    volumes:
      - ./database.sqlite:/data/database.sqlite
